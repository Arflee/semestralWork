#pragma once

/**
 * @brief enum class used in MergeOperation to determine how the matrices will be merged.
 * Can be used for own purposes
*/
enum class MergeDirection
{
    None,
    Right,
    Bottom
};